type Query {
    getAllMovies: [Movie]
    getMovieById(id: ID): Movie
}

type Movie {
    id: ID
    name: String
    actor: Actor
    ratings: Int
}

type Actor {
    id: ID
    actor_name: String
}